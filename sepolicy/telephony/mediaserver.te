#telephony specific permissions for mediaserver
unix_socket_connect(mediaserver, mmgr, mmgr)

allow mediaserver modem_audio_tty_device:chr_file rw_file_perms;
allow mediaserver mmgr_socket:sock_file write;
